<!DOCTYPE html>
<html>
    <head>

        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initiall-scale=1 , shrink-to-fit=nio">
        {% block javascripts %}
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"/>
        {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <title>{% block title %}Главная{% endblock %}</title>

        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
            {#{{ encore_entry_link_tags('app') }}#}
            <header class="p-3 bg-dark text-white">
                <div class="container">
                    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
                            <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
                        </a>

                        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                            <li><a href="{{ path('app_homepage') }}" class="nav-link px-2 text-white">Главная</a></li>
                            <li><a href="{{ path('app_news') }}" class="nav-link px-2 text-white">Новости</a></li>
                            <li><a href="{{ path('app_info') }}" class="nav-link px-2 text-white">Полезная информация</a></li>
                            <li><a href="{{ path('app_about') }}" class="nav-link px-2 text-white">Контакты</a></li>
                            <li><a href="#" class="nav-link px-2 text-white">Для чего этот сайт?</a></li>
                        </ul>

                        <div class="text-end">
                            <button type="button" class="btn btn-outline-light me-2">Вход</button>
                            <button type="button" class="btn btn-warning">Зарегестрироваться</button>
                        </div>
                    </div>
                </div>
            </header>
        {% endblock %}

{#        {% block javascripts %}#}
{#            <script src="https://ajax.googleapis.com/ajax/libs/cesiumjs/1.78/Build/Cesium/Cesium.js"></script>#}
{#            {{ encore_entry_script_tags('app') }}#}
{#        {% endblock %}#}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
    <footer class="pt-4 my-md-5 pt-md-5 border-top">
        <div class="row">
            <div class="col-12 col-md">
                <small class="d-block mb-3 text-muted">© 2021-20...</small>
            </div>
            <div class="col-6 col-md">
                <h5>Предметная область</h5>
               <p> wikipedia</p>
            </div>
            <div class="col-6 col-md">
                <h5>Resources</h5>
                <p>документация</p>
            </div>
            <div class="col-6 col-md">
                <h5>Контакты</h5>
                <a href="vk.ru">Разработчик</a>
            </div>
        </div>
    </footer>

</html>
